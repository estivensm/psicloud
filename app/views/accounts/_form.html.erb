


 

<%= form_for(account) do |f| %>
  <div class="alert alert-danger" style="display:none">
    <ul>
    
    </ul>
  </div>
      <div class="modal-body">

<div class="row">
    <div class="col-md-6">
        <div class="form-group">
             <p>Nombre (Empresa/Persona)</p>
              <div class="input-group">
                <span class="input-group-addon" style="background-color: #5BBBE1;"><i class="fas fa-user" style="color: #f1f1f1;"></i></span>
               <%= f.text_field :name ,  class: "form-control"%>
            </div>
        </div>
    </div>
    <div class="col-md-6">
      <div class="form-group">
            <p>Nit o Cédula</p>
              <div class="input-group">
                <span class="input-group-addon" style="background-color: #5BBBE1;"><i class="fas fa-address-card" style="color: #f1f1f1;"></i></span>
            <%= f.text_field :nit ,  class: "form-control"%>
        </div>
      </div>
    </div>
        <div class="col-md-6">
          <div class="form-group">
             <p>Teléfono</p>
              <div class="input-group">
                <span class="input-group-addon" style="background-color: #5BBBE1;"><i class="fas fa-phone" style="color: #f1f1f1;"></i></span>
                  <%= f.text_field :phone,  class: "form-control" %>
              </div>
          </div>
    </div>
    <div class="col-md-6">
          <div class="form-group">
            <p>Dirección</p>
              <div class="input-group">
                <span class="input-group-addon" style="background-color: #5BBBE1;"><i class="fas fa-user" style="color: #f1f1f1;"></i></span>
                 <%= f.text_field :address,  class: "form-control" %>
              </div>
          </div>
    </div>
    <div class="col-md-6">
        <div class="form-group">
            <p>Email</p>
              <div class="input-group">
                <span class="input-group-addon" style="background-color: #5BBBE1;"><i class="fas fa-at" style="color: #f1f1f1;"></i></span>
                  <%= f.text_field :email,  class: "form-control" %>
              </div>
        </div>
    </div>
     <div class="col-md-6">
          <div class="form-group">
            <p>Cuenta</p>
              <div class="input-group">
                <span class="input-group-addon" style="background-color: #5BBBE1;"><i class="fas fa-user-circle" style="color: #f1f1f1;"></i></span>
           <%= f.select :account_type_id,
                  @account_type.map { |u| ["#{u.name}", u.id] },
                             { include_blank: true },
                             { class: 'form-control'  }
                  %>
          </div>
        </div>
    </div>
    <div class="col-md-6">
            <div class="form-group">
    <p>Logo</p>
    <br>
    <%= f.file_field :logo,  class: "form-control inputfile", id: 'file' %>
    <label for="file" ><i class="fa fa-upload" aria-hidden="true"></i>

 <span class="levidencia" style = "margin-right:10px;">Adjuntar </span> <%if @account.logo.blank?   != true %>
<%= f.check_box :remove_logo %> 
   <%= f.label "Eliminar", style: "color:red!important" %></label>

   <%end%>
   </div>
    </div>
  
</div>
  






<br>
<hr>
  <div class="form-group text-center">
   <%= f.submit "Guardar", class: 'btn btn-primary btnpad' %>
   <button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
  </div>
<% end %>



 <script>

if ("<%=action_name%>" == "edit" && <%= @account.logo.blank?  %> != true)
{
$('.levidencia').html(" <%=@account.logo.file.filename if @account.logo.file %> ")

}



'use strict';

;( function ( document, window, index )
{
  var inputs = document.querySelectorAll( '.inputfile' );
  Array.prototype.forEach.call( inputs, function( input )
  {
    var label  = input.nextElementSibling,
      labelVal = label.innerHTML;

    input.addEventListener( 'change', function( e )
    {
      var fileName = '';
      if( this.files && this.files.length > 1 )
        fileName = ( this.getAttribute( 'data-multiple-caption' ) || '' ).replace( '{count}', this.files.length );
      else
        fileName = e.target.value.split( '\\' ).pop();

      if( fileName )
        label.querySelector( 'span' ).innerHTML = fileName;
      else
        label.innerHTML = labelVal;
    });

    // Firefox bug fix
    input.addEventListener( 'focus', function(){ input.classList.add( 'has-focus' ); });
    input.addEventListener( 'blur', function(){ input.classList.remove( 'has-focus' ); });
  });
}( document, window, 0 ));

   
</script>



<style>
.modal-body p
{
  margin: 0px;
}
.inputfile {
  width: 0.1px;
  height: 0.1px;
  opacity: 0;
  overflow: hidden;
  position: absolute;
  z-index: -1;
}

.inputfile + label {
    font-size: 1em;
    border: 1px solid #d0d0d0;
    border-radius: 5px;
    font-weight: 100!important;
    color: gray;
    background-color: #f9f9f9;
    padding: 4px 20px;
    display: inline-block;
    border-bottom: 2px solid gray;
}

.inputfile:focus + label,
.inputfile + label:hover {
    
}

.inputfile + label {
  cursor: pointer; /* "hand" cursor */
}

.inputfile:focus + label {
  outline: 1px dotted #000;
  outline: -webkit-focus-ring-color auto 5px;
}
.shi1
   {
   width: 150px !important;
   display: inline-block;
   height:  30px !important;
   color:gray !important;
   font-size: 10px !important;
   padding: 6px 2px;
      }
         .busqueda
   {
        display: block;
    padding: 10px;
    background: #e8e8e8;
   }
  

</style>

