$(document).on('turbolinks:load',function() {

     $('#field_state').bootstrapToggle({
         on: 'Activo',
         off: 'Inactivo'
       });
   
     $('#estado').bootstrapToggle({
         on: 'Activo',
         off: 'Inactivo'
       });
   
  });
   