$(document).on('turbolinks:load',function() {

     $('#observation').bootstrapToggle({
         on: 'Activo',
         off: 'Inactivo'
       });
   



     $('#evolution').bootstrapToggle({
         on: 'Activo',
         off: 'Inactivo'
       });
   

     $('#description').bootstrapToggle({
         on: 'Activo',
         off: 'Inactivo'
       });
   
  });
   